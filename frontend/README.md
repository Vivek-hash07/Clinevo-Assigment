# Clinevo frontend

Angular 19 reviewer UI. Local port **8000**. Full setup, env vars, and what works / what does not are in the repo-root [README.md](../README.md).

```bash
npm install
npm start
```

Open http://localhost:8000 (use `localhost`, not `127.0.0.1`).
